[package]
name = "shell-switcher"
version = "0.1.0"
edition = "2021"
description = "Runtime switch between desktop shells (Noctalia/DMS/caelestia/Persona) on Hyprland/niri"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
